import '../styles/globals.css';
import type { AppProps } from 'next/app';

function SapteTreizeci({ Component, pageProps }: AppProps) {
  return <Component {...pageProps} />;
}

export default SapteTreizeci;
